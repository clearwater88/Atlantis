function [res] = getAppPatches(part,params)

    partSize = size(part);
    maxDim = max(partSize);
    
    res = zeros([maxDim,maxDim,numel(params.orientationsUse)]);
    
    for (i=1:numel(params.orientationsUse))
       res  
    end

end

