function sampleParticles(particles,particleProbs,like,counts,allProbMaps,saliencyMap,cellParams,params,ruleStruct,templateStruct)

    [type,cellLoc] = getNextSaliencyLoc(saliencyMap,particles{1});

    for(n=1:params.nParticles)
       
    end


end

